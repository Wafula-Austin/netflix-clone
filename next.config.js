/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : [
            'image.tmdb.org',
            'assets.nflxext.com',
            'robohash.org'
        ]
    }
}

module.exports = nextConfig
